@model BoardGameReviews.Models.CategoryDeleteViewModel

@{
    ViewData["Title"] = "Delete Category";
}

<section class="game-page game-delete-page">
    <div class="game-hero mb-4 d-flex justify-content-between align-items-start gap-3 flex-wrap">
        <div>
            <p class="eyebrow mb-2">Delete Category</p>
        </div>
        <a asp-action="Index" class="btn game-btn-outline">Back to List</a>
    </div>

    <div class="game-card">
        <div class="card-header">Delete Confirmation</div>
        <div class="card-body">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            <p><strong>@Model.Category.Name</strong></p>
            <p class="mb-4">Games: @Model.GameCount</p>

            @if (!Model.CanDelete)
            {
                <p class="text-danger mb-0">Delete is not allowed because this category has related games.</p>
            }
            else
            {
                <form asp-action="Delete" method="post">
                    <input type="hidden" name="id" value="@Model.Category.Id" />
                    <button type="submit" class="btn game-btn-danger">Confirm Delete</button>
                </form>
            }
        </div>
    </div>
</section>
