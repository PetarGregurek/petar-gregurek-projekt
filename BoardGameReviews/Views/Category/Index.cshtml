@model BoardGameReviews.Models.CategoryIndexViewModel

@{
    ViewData["Title"] = "Categories";
}

<section class="game-page game-list-page">
    <div class="game-hero mb-4">
        <p class="eyebrow mb-2">BoardGameReviews</p>
        <h1 class="mb-2">Categories</h1>
        @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
        {
            <div class="status-banner mb-3">@Model.StatusMessage</div>
        }
    </div>

    <div id="categories" class="entity-block game-table-wrap mb-4">
        <div class="entity-header">Categories</div>
        <div class="list-toolbar">
            <div class="list-toolbar__search">
                <label class="form-label" for="category-search">Search</label>
                <input id="category-search"
                       class="form-control list-search-input"
                       type="search"
                       placeholder="Search by name or description"
                       value="@Model.Search"
                       data-ajax-search
                       data-search-url="@Url.Action("Index", "Category")"
                       data-search-target="#categories-table-body" />
            </div>
            <a class="btn game-btn-primary list-toolbar__action" asp-action="Create">New Category</a>
        </div>
        <div class="table-responsive">
            <table class="table game-table align-middle mb-0">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Age Group</th>
                        <th>Difficulty</th>
                        <th>Popularity</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="categories-table-body">
                    @await Html.PartialAsync("_CategoryTableRows", Model)
                </tbody>
            </table>
        </div>
    </div>
</section>
