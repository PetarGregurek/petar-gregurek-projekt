@model BoardGameReviews.Models.UserFormViewModel

@{
    ViewData["Title"] = "Edit User";
}

<section class="game-page game-form-page">
    <div class="game-hero mb-4 d-flex justify-content-between align-items-start gap-3 flex-wrap">
        <div>
            <p class="eyebrow mb-2">Edit User</p>
        </div>
        <a asp-action="Index" class="btn game-btn-outline">Back to List</a>
    </div>

    <div class="game-card">
        <div class="card-header">User Editing</div>
        <div class="card-body">
            <form asp-action="Edit" method="post">
                <input asp-for="Input.Id" type="hidden" />
                @await Html.PartialAsync("_UserForm", Model)
            </form>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
