@model BoardGameReviews.Models.PublisherIndexViewModel

@{
    ViewData["Title"] = "Publishers";
}

<section class="game-page game-list-page">
    <div class="game-hero mb-4">
        <p class="eyebrow mb-2">BoardGameReviews</p>
        <h1 class="mb-2">Publishers</h1>
        @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
        {
            <div class="status-banner mb-3">@Model.StatusMessage</div>
        }
    </div>

    <div id="publishers" class="entity-block game-table-wrap mb-4">
        <div class="entity-header">Publishers</div>
        <div class="list-toolbar">
            <div class="list-toolbar__search">
                <label class="form-label" for="publisher-search">Search</label>
                <input id="publisher-search"
                       class="form-control list-search-input"
                       type="search"
                       placeholder="Search by name or country"
                       value="@Model.Search"
                       data-ajax-search
                       data-search-url="@Url.Action("Index", "Publisher")"
                       data-search-target="#publishers-table-body" />
            </div>
            <a class="btn game-btn-primary list-toolbar__action" asp-action="Create">New Publisher</a>
        </div>
        <div class="table-responsive">
            <table class="table game-table align-middle mb-0">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Country</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="publishers-table-body">
                    @await Html.PartialAsync("_PublisherTableRows", Model)
                </tbody>
            </table>
        </div>
    </div>
</section>
